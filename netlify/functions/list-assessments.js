exports.handler=async function(event){try{const adminPassword=process.env.ADMIN_PASSWORD;if(!adminPassword||event.headers["x-admin-password"]!==adminPassword)return json(401,{error:"Wrong admin password"});const supabaseUrl=process.env.SUPABASE_URL,supabaseKey=process.env.SUPABASE_SECRET_KEY;const res=await fetch(`${supabaseUrl}/rest/v1/assessments?select=id,candidate_name,candidate_email,created_at,score&order=created_at.desc`,{headers:{apikey:supabaseKey,Authorization:`Bearer ${supabaseKey}`}});const data=await res.json();if(!res.ok)return json(500,{error:data.message||"Could not list assessments"});return json(200,{assessments:data})}catch(err){return json(500,{error:err.message})}};function json(statusCode,body){return{statusCode,headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}}
